requests==2.31.0
python-dotenv==1.0.1
pyserial
# Le récepteur envoie désormais les relevés à l'API Flask (POST /api/data)
# via requests, exactement comme tools/simulate.py. InfluxDB n'est plus
# écrit directement depuis lora/ — c'est Flask (app/) qui s'en charge.
